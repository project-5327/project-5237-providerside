import 'package:flutter/material.dart';

class CustomizeDivider extends StatelessWidget {
  const CustomizeDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(
      color: Color(0xffEAEEF0),
      height: 1,
    );
  }
}
